{
  "name": "gesso-sveltia-auth",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Gesso shared Sveltia CMS GitHub OAuth relay — serves /admin login for every artist site.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.84.0"
  }
}
